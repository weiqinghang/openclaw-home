# Handover

## 维护说明

- 只有当用户明确要求“更新 handover”时，才修改本文件。
- 只有当用户明确要求“读取 handover”时，才主动读取本文件。
- 每次维护本文件时，必须记录：
  - 当前维护时间戳
  - 维护时仓库最近一个 commit 版本号
- 每次维护本文件后，单独提交一次，不与其他改动混合提交。

## 最近维护

- 时间：2026-03-10 11:33:26 CST
- 基线 commit：`b8369fa`

## 仓库目标

这个仓库维护本机 OpenClaw 实例的：

- 配置层
- Agent 人格与长期资产
- 共享技能
- Hook
- 运维文档

运行时数据不放仓库内，而是外置到数据根目录。

## 当前架构

### 1. 多 Agent

当前已有 4 个 Agent：

- `wukong`
- `taibai`
- `guanyin`
- `guichengxiang`

绑定方式：

- 1 个飞书 App
- 1 个 `accountId`
- 1 个 `agentId`

主配置：

- `openclaw.json`

### 2. 多用户

当前已跑通：

- pairing
- 每 Agent 私有用户目录
- 跨 Agent 低敏共享画像

相关说明：

- `README.md`
- `docs/operator/add-feishu-user.md`

### 3. 运行态外置

默认数据根目录：

- `~/Documents/OpenClawData`

统一修改入口：

- `scripts/set-data-root.js`

## 当前关键成果

### 1. OpenClaw 多 Agent / 多用户基础架构已成型

已完成：

- 多 App 路由
- 多 Agent 目录隔离
- users / sessions / logs 外置
- shared-users 共享画像
- LaunchAgent secrets 管理

参考：

- `README.md`
- `docs/operator/add-agent.md`

### 2. 太白工作流已落地

已完成：

- 报价单生成
- SKU 库提案式维护
- 飞书多维表格接入
- 报价单文件回传

关键文件：

- `core/skills/trade-operations-workflow/SKILL.md`
- `scripts/trade_parse_request.py`
- `scripts/trade_generate_quote_from_bitable.py`
- `scripts/feishu_trade_bitable_upsert.js`
- `hooks/taibai-workflow/handler.js`

### 3. docs 已按维护用途重组

当前结构：

- `docs/index.md`
- `docs/operator/`
- `docs/architecture/`
- `docs/incidents/`
- `docs/agents/`

### 4. 龟丞相专项问题已形成经验

已沉淀：

- workspace 入口文件必须实体化
- 副作用测试必须同时清 workspace 与 session
- `/new` 不是生产级硬重置

参考：

- `docs/incidents/agent-memory-pollution.md`
- `docs/operator/side-effect-test-reset.md`
- `docs/agents/guichengxiang/retrospective.md`

## 当前关键判断

### 1. 真正重的不是人格文件，而是 skills snapshot

当前启动成本的大头，是运行时注入的技能快照。  
人格文件只是次级成本。

### 2. 4 个 Agent 的约束完整度不均衡

- `taibai`：最完整
- `guichengxiang`：次完整
- `wukong`：缺 `BOOTSTRAP.md`
- `guanyin`：缺 `BOOTSTRAP.md`，工具边界偏薄

### 3. Agent 间协作仍停留在人设层

虽然文案里有“转交专家 / 转交 Codex”，但还没有真正接上稳定的 Agent-to-Agent 生产链路。

### 4. 会驱动 Agent 的文档，必须和人类说明文档分开

否则长期维护后，Agent 输入边界会越来越脏。

## 当前关键文档

- `README.md`
- `CHANGELOG.md`
- `openclaw.json`
- `scripts/with-openclaw-secrets.sh`
- `docs/index.md`
- `docs/operator/add-agent.md`
- `docs/operator/add-feishu-user.md`
- `docs/operator/side-effect-test-reset.md`
- `docs/architecture/trade-data-model.md`
- `docs/architecture/agent-context-optimization-plan.md`
- `docs/incidents/agent-memory-pollution.md`
- `docs/agents/guichengxiang/retrospective.md`

## 当前建议

如果下一轮继续推进，优先顺序应是：

1. 压缩 Agent 启动上下文
2. 给 `wukong`、`guanyin` 补 `BOOTSTRAP.md`
3. 清理 `wukong/MEMORY.md` 旧噪音
4. 定义最小 Agent 协作协议

细节见：

- `docs/architecture/agent-context-optimization-plan.md`
