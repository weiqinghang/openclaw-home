# Handover

## 维护说明

- 只有当用户明确要求“更新 handover”时，才修改本文件。
- 只有当用户明确要求“读取 handover”时，才主动读取本文件。
- 每次维护本文件时，必须记录：
  - 当前维护时间戳
  - 维护时仓库最近一个 commit 版本号
- 每次维护本文件后，单独提交一次，不与其他改动混合提交。

## 最近维护

- 时间：2026-03-15 16:49:00 CST
- 基线 commit：`2668dbc`

## 仓库目标

这个仓库维护本机 OpenClaw 实例的：

- 配置层
- Agent 人格与长期资产
- 共享技能
- Hook
- 运维文档

运行时数据不放仓库内，而是外置到数据根目录。

## 当前架构

### 1. 多 Agent

当前 OpenClaw 内已有 5 个 Agent：

- `wukong`
- `taibai`
- `guanyin`
- `guichengxiang`
- `laojun`

Claude 内另有 3 个专家 Agent：

- `architect`
- `fullstack-engineer`
- `reviewer`

绑定方式：

- 1 个飞书 App
- 1 个 `accountId`
- 1 个 `agentId`

主配置：

- `openclaw.json`

### 2. 多用户

当前已跑通：

- pairing
- 每 Agent 私有用户目录
- 跨 Agent 低敏共享画像

相关说明：

- `README.md`
- `docs/operator/add-feishu-user.md`

### 3. 运行态外置

默认数据根目录：

- `~/Documents/OpenClawData`

统一修改入口：

- `scripts/set-data-root.js`

## 当前关键成果

### 1. OpenClaw 多 Agent / 多用户基础架构已成型

已完成：

- 多 App 路由
- 多 Agent 目录隔离
- users / sessions / logs 外置
- shared-users 共享画像
- LaunchAgent secrets 管理

参考：

- `README.md`
- `docs/operator/add-agent.md`

### 2. 太白工作流已落地

已完成：

- 报价单生成
- SKU 库提案式维护
- 飞书多维表格接入
- 报价单文件回传
- skill 脚本已迁入标准 skill 目录

关键文件：

- `core/skills/trade-operations-workflow/SKILL.md`
- `core/skills/trade-operations-workflow/scripts/trade_parse_request.py`
- `core/skills/trade-operations-workflow/scripts/trade_generate_quote_from_bitable.py`
- `core/skills/trade-operations-workflow/scripts/feishu_trade_bitable_upsert.js`
- `hooks/taibai-workflow/handler.js`

### 3. Agent 启动上下文第一轮压缩已落地

已完成：

- `wukong`、`guanyin` 已补 `BOOTSTRAP.md`
- `guanyin/TOOLS.md` 已补工具边界
- `wukong/MEMORY.md` 已清旧 EvoMap 噪音
- `scripts/sync-agent-workspace.js` 已落地
- workspace 入口文件已改为实体同步
- `workspace/skills` 已按 Agent 白名单收缩

### 4. skill 结构标准已固化

已完成：

- 新增 `docs/architecture/skill-structure-standard.md`
- 后续 skill 统一按 `SKILL.md + scripts/ + references/ + assets/`
- 仓库级 `scripts/` 只保留公共运维或跨 skill 基础设施

### 5. docs 已按维护用途重组

当前结构：

- `docs/index.md`
- `docs/operator/`
- `docs/architecture/`
- `docs/incidents/`
- `docs/agents/`

### 6. 龟丞相专项问题已形成经验

已沉淀：

- workspace 入口文件必须实体化
- 副作用测试必须同时清 workspace 与 session
- `/new` 不是生产级硬重置

参考：

- `docs/incidents/agent-memory-pollution.md`
- `docs/operator/side-effect-test-reset.md`
- `docs/agents/guichengxiang/retrospective.md`

### 7. 多 Agent 全局安全公约已落地

已完成：

- 新增全局安全公约
- 新增 `openclaw-safe.sh` 高危命令保险丝
- 4 个 Agent 的 `BOOTSTRAP.md` / `TOOLS.md` 已接入
- `scripts/sync-agent-workspace.js` 会同步 `SHARED-SAFETY.md`
- 自毁型运行时操作事故已形成复盘

参考：

- `docs/agents/shared-safety-charter.md`
- `scripts/openclaw-safe.sh`
- `docs/incidents/agent-runtime-self-destruction.md`

### 8. ACP -> Codex 通信已打通并完成 2026.3.13 兼容收口

已完成：

- LaunchAgent 已补齐 4 个 Agent 的 Feishu secrets 与 gateway token
- gateway 冷启动缺 secrets 导致 ACP 全挂的问题已消除
- 项目级 `.acpxrc.json` 已固定到仓库内本地 `codex-acp`
- 新增 `scripts/acpx-codex.sh` 作为仓库标准入口
- 已验证 one-shot 与持久 session 都可通过 ACP 直连 Codex
- `guichengxiang -> ACP -> Codex` 已实测返回 `INTERNAL_PONG`
- 新增 `scripts/check-acp-runtime.sh` 作为一键验收入口
- 已适配 `OpenClaw 2026.3.13`
- `scripts/openclaw-acp-spawnedby-patch.js` 已支持识别 `upstream_fixed`
- `scripts/resolve-acpx-bin.sh` 已适配新版 `acpx` bundled 安装路径
- `scripts/check-acp-runtime.sh` 已适配新版 ACP 输出

当前限制：

- 当前 `OpenClaw 2026.3.13` 下，spawnedBy 问题已是上游修复态
- 后续升级后仍必须重跑 ACP 验收，确认新版本是否继续保持 `upstream_fixed`
- 项目级 ACP 仍依赖本机 `acpx` 可执行入口解析与本地安装能力

### 9. 老君已升级为首席产品官入口，项目 bootstrap 已落地

已完成：

- `laojun / 太上老君` 已明确为“首席产品官入口”
- 项目级 bootstrap 脚本已落地：`scripts/create-project-agent.js`
- 项目注册表已落地：`ops/project-registry.json`
- 项目骨架目录已落地：
  - `agents/projects/<projectId>/`
  - `projects/<projectId>/docs/`
- 已实测打通 1 个真实项目群：
  - 项目：`test-project`
  - 群：`oc_d41755f2043cc0dd8036e4c3e1d1b3b8`
- 老君使用文档已落地：
  - `docs/agents/laojun/usage.md`

### 10. 项目 bootstrap 已切到仓库外真实项目根

已完成：

- 项目根默认外置到 `~/Documents/OpenClawData/projects/<projectId>/`
- 项目 Agent 静态资产放到 `~/Documents/OpenClawData/projects/<projectId>/agent/`
- 项目 Agent 运行态放到 `~/Documents/OpenClawData/projects/<projectId>/.runtime/openclaw/`
- 支持旧项目 `gitRemote` + 建分支 + upstream 初始化
- 内部 GitLab `git.tarsocial.com` 分支命名固定为：
  - `feature/<spec>-<topic>`
- 项目 bootstrap 测试已补齐并通过

参考：

- `scripts/create-project-agent.js`
- `scripts/create-project-agent.test.js`
- `docs/operator/project-agent-bootstrap.md`

### 11. 老君第一阶段 UI 交付链路已接通

已完成：

- 老君人格已补 UI/UX、页面设计、原型任务收敛口径
- 老君默认将 UI 设计执行路由给 `Codex`
- 老君 workspace 已补：
  - `ui-ux-pro-max`
  - `playwright`
  - `figma`
  - `figma-implement-design`
- 项目根已补设计与原型目录：
  - `docs/design/`
  - `design/`
  - `prototype/`
- `star-steps` 已验证：
  - spec 可落盘
  - low-fi brief 可落盘
  - 原型目录可预置

参考：

- `agents/laojun/AGENTS.md`
- `agents/laojun/BOOTSTRAP.md`
- `agents/laojun/TOOLS.md`
- `scripts/sync-agent-workspace.js`
- `scripts/sync-agent-workspace.test.js`
- `docs/operator/laojun-ui-delivery.md`
### 12. Spec-kit / OpenSpec / Superpowers / XP 方法论已接入

已完成：

- `spec-kit-workflow`
- `openspec-workflow`
- `extreme-programming`
- `wukong` / `guichengxiang` 已强制用户先在两种 workflow 中二选一

原则：

- 新建类：`Spec-kit`
- 变更类：`OpenSpec`
- 执行默认走 `Superpowers + XP`

## 当前关键判断

### 1. 真正重的不是人格文件，而是 skills snapshot

当前启动成本的大头，是运行时注入的技能快照。  
人格文件只是次级成本。

### 2. 4 个 Agent 的首轮约束已基本补齐

- `taibai`：最完整
- `wukong`：已补 `BOOTSTRAP.md`，长期记忆已清噪
- `guanyin`：已补 `BOOTSTRAP.md`，工具边界已补
- `guichengxiang`：仍较完整

### 3. Agent 间协作开始进入真实链路阶段

当前至少已有一条真实可用链路：

- `guichengxiang -> ACP -> Codex`

补充：

- 项目级 ACP 直连 Codex 已可用
- OpenClaw 内部 ACP runtime 在 `2026.3.13` 下已验证通过
- 但每次 `openclaw update` 后仍必须重跑 `scripts/check-acp-runtime.sh`

### 4. 当前老君的主要问题已收口到两类

第一类：**回复消息出不来 / 出现在错误显示层**

- 现象：老君已生成完整回复并落盘，但飞书群里只看到部分回复或只看到线程回复
- 当前判断：执行层正常，问题集中在飞书显示层 / reply 模式 / 消息可见性
- 已证据：
  - transcript 中有完整 final answer
  - 文件已写入项目目录
  - 群里未完整看到结果

第二类：**没有使用专用 Agent 做事**

- 现象：老君当前会自己整理 spec / brief，而不是稳定转给项目协调员或 Codex
- 当前判断：人格与技能已补口径，但实际会话仍可能沿用旧任务态或默认直接执行
- 需要下一轮继续把“收敛 -> 路由 -> 交付”硬规则前移

### 5. skill 目录必须自包含，不要把业务实现散在仓库根

`SKILL.md`、业务脚本、reference、asset 应优先同目录收口。  
仓库级 `scripts/` 只留公共基础设施。

### 6. 会驱动 Agent 的文档，必须和人类说明文档分开

否则长期维护后，Agent 输入边界会越来越脏。

### 7. 多 Agent 安全底线不能只散落在各自人格文件

仅靠单个 Agent 的提示词，拦不住运行态自毁命令。  
需要“全局公约 + 技术保险丝”双层约束。

### 8. LaunchAgent secrets 同步必须脚本化

当前已新增：

- `scripts/sync-launchagent-secrets.js`

用途：

- 从 `secrets.local.json` 同步 `OPENCLAW_GATEWAY_TOKEN` 与 `FEISHU_*_APP_SECRET`
- 同步到 LaunchAgent plist 与 `launchctl`
- 可选重启 gateway 并顺手做 health 检查

补充：

- 修改 `secrets.local.json` 后，不应再手工逐个 `launchctl setenv`
- 优先执行：
  - `node scripts/sync-launchagent-secrets.js --restart-gateway --check-health`

### 9. 多账号 Feishu 配置已收口到账号级

当前调整：

- 顶层 `channels.feishu.groupAllowFrom/groups/userAllowlist/groupPolicy/requireMention` 冗余项已移除
- 已补 `channels.feishu.defaultAccount`
- 业务群放行、免 `@`、allowFrom 统一收口到 `channels.feishu.accounts.<accountId>`

补充：

- `doctor` 旧的 `accounts.default` 迁移提示已消除
- 项目群写入逻辑已统一走账号级路径

### 10. 最小回归清单已落地

当前已新增：

- `docs/operator/minimal-regression-checklist.md`

用途：

- 作为每次 `openclaw update` 或兼容层调整后的最小人工/脚本复验入口
- 当前最关键的一键验收仍是：
  - `./scripts/check-acp-runtime.sh`

### 11. Feishu 多账号模式下，群 allowlist 必须写到账号级

这是 2026-03-14 新验证出的关键坑。

错误直觉：

- 只写顶层 `channels.feishu.groupAllowFrom`
- 只写顶层 `channels.feishu.groups.<groupId>`

在多账号 Feishu 模式下，这样可能出现：

- `config hot reload applied`
- 但运行时仍报：
  - `group oc_xxx not in groupAllowFrom (groupPolicy=allowlist)`

本次确认的正确写法：

- `channels.feishu.accounts.<accountId>.groupPolicy`
- `channels.feishu.accounts.<accountId>.groupAllowFrom`
- `channels.feishu.accounts.<accountId>.groups.<groupId>`

其中：

- `groupPolicy = "allowlist"`
- `groupAllowFrom += groupId`
- `groups.<groupId>.requireMention = false`
- `groups.<groupId>.allowFrom = [ownerUserId]`

### 12. 老君 / 项目维护 Agent / 共享专家 三层架构已落地第一版

已完成：

- 老君定位收紧为：
  - 首席产品官入口
  - 项目初始化守门人
  - 对人汇报层
- 项目维护 Agent 模板已升级为项目内中枢：
  - 持有 spec / roadmap / delivery-state / decisions / risks / task-package
  - 负责项目内 checkpoint、验收、回写、风险升级
- 共享专家固定为：
  - `uiux-designer`
  - `architect`
  - `fullstack-engineer`
  - `reviewer`
- 老君已禁止：
  - `sessions_spawn`
  - `write`
  - `edit`
  - `apply_patch`
- 大任务模块化、分批次交付、禁止巨大单文件，已写入老君与项目 Agent 模板

### 13. `star-steps` 项目维护 Agent 已补建并打通 handoff

已完成：

- `star-steps` 已写入：
  - `openclaw.json`
  - `ops/project-registry.json`
- 项目维护 Agent 资产位于：
  - `~/Documents/OpenClawData/projects/star-steps/agent/`
- runtime workspace 位于：
  - `~/Documents/OpenClawData/projects/star-steps/.runtime/openclaw/workspace/`
- 已验证：
  - `openclaw agent --agent star-steps` 可直连 Claude 返回
  - 老君可通过 `sessions_send(agent:star-steps:main)` 把项目内问题转交给 `star-steps`

重要口径：

- 项目维护 Agent 的**路径公约**在：
  - `~/Documents/OpenClawData/projects/<projectId>/agent/`
  - `~/Documents/OpenClawData/projects/<projectId>/.runtime/openclaw/workspace/`
- 不应再用 `agents_list` 或 `~/Documents/OpenClawData/agents/<projectId>/` 去判断项目 Agent 是否存在
- 但 **session store 当前仍落在全局**：
  - `~/Documents/OpenClawData/agents/<projectId>/sessions/`
  - 这是 OpenClaw 当前架构行为，不是项目未初始化

### 14. Claude provider/auth 已补全到 gateway 级

已完成：

- 顶层 `openclaw.json` 已补：
  - `auth.profiles.anthropic-proxy:default`
  - `auth.order.anthropic-proxy`
  - `models.providers.anthropic-proxy`
- 项目 Agent 模板已补默认 Claude 配置：
  - `agent/models.json`
  - `agent/auth-profiles.json`

原因：

- 之前 `star-steps` 直跑可用，但经 gateway / `sessions_send` handoff 会失败
- 根因是 gateway 层缺少全局 `anthropic-proxy` 配置

### 15. 今日复盘已沉淀

新增：

- `docs/incidents/laojun-uiux-delivery-retrospective-2026-03-15.md`

已补充内容：

- 老君越权写文件问题
- UI/UX 专家链路失真问题
- 项目维护 Agent 查找口径
- gateway / auth 问题
- `sessions_send` 与自动播报噪音

## 当前关键文档

- `README.md`
- `CHANGELOG.md`
- `openclaw.json`
- `scripts/check-acp-runtime.sh`
- `scripts/openclaw-acp-spawnedby-patch.js`
- `scripts/with-openclaw-secrets.sh`
- `docs/index.md`
- `docs/operator/add-agent.md`
- `docs/operator/add-feishu-user.md`
- `docs/operator/feishu-group-debug.md`
- `docs/operator/feishu-project-group-init-runbook.md`
- `docs/operator/openclaw-acp-runtime-patch.md`
- `docs/operator/side-effect-test-reset.md`
- `docs/operator/project-agent-bootstrap.md`
- `docs/architecture/trade-data-model.md`
- `docs/architecture/agent-context-optimization-plan.md`
- `docs/architecture/skill-structure-standard.md`
- `docs/agents/shared-safety-charter.md`
- `docs/incidents/agent-memory-pollution.md`
- `docs/incidents/agent-runtime-self-destruction.md`
- `docs/incidents/laojun-uiux-delivery-retrospective-2026-03-15.md`
- `docs/agents/guichengxiang/retrospective.md`

## 未尽任务

1. **多条消息一股脑返回** 仍未解决
   - 当前虽已打通 `laojun -> star-steps -> laojun`
   - 但 agent-to-agent 自动播报仍会产生：
     - `NO_REPLY`
     - `REPLY_SKIP`
     - `ANNOUNCE_SKIP`
   - 群里仍可能出现多段状态集中返回，而不是自然的逐步 checkpoint
   - 这是当前最明确的未收口项

2. **项目维护 Agent 自动播报噪音** 仍需框架层收口
   - 现在 `sessions_send` 后，除了业务回复，还会有自动播报回流
   - 需要后续区分：
     - 给上游 agent 的回包
     - 面向人类时间线的消息

3. **项目 Agent session store 与项目 runtime 路径分裂**
   - 项目资产在 `projects/<projectId>/`
   - sessions 在 `agents/<projectId>/sessions/`
   - 这虽然已识别，但还未统一

4. **`star-steps` 项目内运营工件仍为空**
   - `roadmap.md`
   - `delivery-state.md`
   - `current-iteration.md`
   - `decisions.md`
   - `risks.md`
   - `task-package.md`
   - 当前只是识别了问题，还没回写

5. **高保真原型仍未完成**
   - `prototype/high-fi/pages/` 仍为空
   - 只完成了 CSS / JS / SVG 骨架
   - 后续应按批次继续走 `uiux-designer`

6. **老君对项目内问题的总结仍偏长**
   - 主链路已对
   - 但当前对人总结还比较重，后续可继续压缩为更高密度输出

## 当前建议

如果下一轮继续推进，优先顺序应是：

1. 先收口**多条消息一股脑返回**
   - 排查 `sessions_send` / 自动播报 / provenance 回流
   - 目标：项目维护 Agent 做到哪回到哪，而不是最后集中刷屏
2. 回写 `star-steps` 的运营工件
   - roadmap / delivery-state / current-iteration / decisions / risks / task-package
3. 继续推进 `star-steps` 高保真原型
   - 严格按批次走 `uiux-designer`
4. 继续验证 `wukong`、`guanyin`、`taibai` 首轮口径与技能白名单效果
5. 把 ACP 升级后检查、补丁重放、冒烟验证纳入固定运维流程
6. 继续把高危运行态命令拦截前移到更底层权限层
7. 定义最小 Agent 协作协议与项目 Agent 回包协议

细节见：

- `docs/architecture/agent-context-optimization-plan.md`
